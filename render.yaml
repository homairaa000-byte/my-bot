services:
  - type: web
    name: quran-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn bot:app --workers=1 --threads=1 --timeout 120"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: "10000"
